Give me a diagram for my AI-use case: the user query is sent to a prompt guardrail node, and then it goes to make an LLM API call through gateway. If successful, it goes through an output guardrail to the user.

Make my family tree. I am Gigel, my Mom is Alice, my father is Bob. I have 2 brothers. My brother andrew is single. My sister in Law Jannet is married with Victor. My aunts from my father side are both called Jane and their father is called Markus

Create my vacation itinerary: Starting from Bucharest I am going to Budapest for 3 nights and on the way there I will spend the night in Timisoara. After that, I will go in Vienna for 3 nights and if it snows, I will go to Prague for 4 nights, otherwise in Munich. On my way back I will go through Budapest for a night to rest and maybe a night somewhere nice. You can suggest the last stop

Recreate the transformer architecture introduced in the paper called "Attention is all you need"

A modular AI Diagram Generator has a Streamlit UI connected to a 7-stage pipeline. User input first goes through request parsing, then prompt planning, then LLM-based JSON spec generation (with fallback mechanism), followed by schema validation & auto-repair, rendering to vis.js graph format, style postprocessing, and export to JSON/SVG/PNG. The UI includes a chat panel, settings sidebar for interactive canvas and editing nodes/edges.